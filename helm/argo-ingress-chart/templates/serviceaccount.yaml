{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-sa
  labels:
    {{- include "argo-ingress-chart.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
{{- end }}
